# %%writefile /kaggle/working/DMNSP/configs/class/tinyimagenet_100-5.yaml
hydra:
      run:
        dir: ./experiments/${scenario}/${dataset}_${initial_increment}-${increment}-${method}
      job:
        chdir: true
    
      job_logging:
        version: 1
        formatters:
          simple:
            format: '%(message)s'

class_order: "/kaggle/working/DMNSP/class_orders/tinyimagenet.yaml"
dataset_root: "/kaggle/working"
workdir: ""
log_path: "metrics.json"
model_name: "ViT-B/16"
prompt_template: "a bad photo of a {}."

batch_size: 64
initial_increment: 100
increment: 5
# fixed here======================
task_num: 21
scenario: "class"
dataset: "tinyimagenet"

weight_decay: 0.0
l2: 0
ce_method: 0

method: "DMNSP"
lr: 1e-3
ls: 0.0


visual_clsf_lr: 0.0005
visual_clsf_batch_size: 64
visual_clsf_epochs: 3
a: 1
b: 4